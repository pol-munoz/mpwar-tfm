{% extends 'study/base.html.twig' %}

{% block title %}{{ study.name }} - Kunlabo{% endblock %}

{% block body %}
    <iframe class="Study-frame" src="{{ asset(engine.mainUrl) }}" title="{{ study.name }}"></iframe>
{% endblock %}